'use strict';
const EventSourceBase = require('../src/dynamodb/EventSourceBase');
const StreamListener = require('../src/dynamodb/StreamListener');

module.exports = {
  EventSourceBase,
  StreamListener
};
